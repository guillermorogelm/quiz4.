Respuestas Quiz 4

Seccion 1
1. b) 50 — pasa por valor, la copia local se modifica pero `num` no cambia.
2. a) 100 — pasa por referencia, `x` es un alias de `num`.
3. a) 100 — pasa el puntero, `x = 100` modifica directamente `num`.
4. b) El valor almacenado en valor (25) — `ptr` desreferencia el puntero.
5. b) Con referencia: `funcion(variable)`. Con puntero: `funcion(&variable)` y dentro se usa `parametro`
6. b) 20 10 — el swap por referencia intercambia los valores originales.
